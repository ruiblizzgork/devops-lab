network:
  version: 2
  ethernets:
    primary:
      match:
        name: "en*"
      dhcp4: false
      addresses:
        - ${ip}/24
      routes:
        - to: default
          via: ${gateway}
      nameservers:
        addresses: [8.8.8.8,1.1.1.1]
